booooks
https://ru.on.worldcat.org/oclc/1276850446
available on
https://people.scs.carleton.ca/%7Epaulv/toolsjewels.html

other book
Ristić, I. (2024). Bulletproof TLS and  
PKI (2nd ed.). Feisty Duck.  
TLS & PKI in practice, in-depth

![[Pasted image 20260601122101.png]]

# TCP spoofing

Just injecting IP packets which seems to originate from another host is insufficient to impersonate that host during a TCP connection, because every TCP segment has a 32bit sequence number. A segment with a sequence number which is out of line will be ignored. That means in order to successfully insert a TCP segment into an existing transmission you need to guess the next sequence number, or your segment will be discarded. This isn't so hard when you can eavesdrop at least on the client, but when you can't, you can only brute-force it. With more simple transport protocols, like UDP for example, you don't have that problem. UDP has no sequence numbers, so unless an upper protocol layer replicates such functionality, you can just insert additional segments which will then be treated as if they were coming from the real host.


# Established TCP Session Hijack
data trasnfer

with <span style="color:rgb(219, 0, 0)">TCP spoofing </span>- guess sequence number
the attacker need to know 
1. source (victim's) IP address (easy as public)
2. destination IP address (easy as public)
3. **source port** (victims's ephemeral port, a bit hard but only 65535 possibilities)
4. **destination port** (server's port)
5. <span style="color:rgb(219, 0, 0)">sequence number</span> - the hard one to guess

with UDP - just insert (that's why DNS spoofing is relatively simple)


![[Pasted image 20260601120348.png]]

TCP
1. connection establishment
2. data transfer
3. connection termination

Connection establsihment
- **SYN**: The active open is performed by the client sending a SYN to the server. The client sets the segment's sequence number to a random value x.
- **SYN-ACK**: In response, the server replies with a SYN-ACK. The acknowledgment number is set to one more than the received sequence number i.e. x+1, and the sequence number that the server chooses for the packet is another random number, y.
- **ACK**: Finally, the client sends an ACK back to the server. The sequence number is set to the received acknowledgment value i.e. x+1, and the acknowledgment number is set to one more than the received sequence number i.e. y+1.

![[Pasted image 20260601121039.png]]


<span style="color:rgb(219, 0, 0)">Hijack TCP connection establishmnet</span>
You also have another problem when you have no existing connection and instead want to establish a new TCP connection which appears to originate from another host. In order to establish a TCP connection, a 3-way handshake is required. (client sends SYN, server sends SYN,ACK, client sends ACK). The SYN,ACK by the server includes a random number which you need for an acceptable ACK. So when you can only send IP packets but not receive any of the packets intended for the spoofed host, you will have to guess this number.

guess seq = y (the random number generated by server)


# Receive Window and injection
TCP spoofing for established connection

In reality, due to the Receive Window, the attacker does not need to send exactly the next  
expected sequence number, but any within the receive window

![[Pasted image 20260601124426.png]]

client and server has a different view
which will cause the connection to reset
but the attack already executed their stuff

now less relevant - more unpredictable sequence numbers


during data transfer, seq numbers increase by the size of data


![[Pasted image 20260601123902.png]]


Telnet vulnerability
check out


# Takeaways from Exercises

know wireshark flags
	to see TCP SYN flag
	`tcp.flags.syn == True

random <span style="color:rgb(219, 0, 0)">ephemeral</span> source ports
	OS assigns new ones per each connection sesh
	avoids collisions — two tabs opening the same website get **different source ports**
	security — makes it harder for attackers to guess the full 4-tuple needed for TCP spoofing/hijacking


scapy can be used to inject a TCP packet in already established TCP connection
	send() just send a packet with nor response
	sr() sends and recieves replays
	sr1() send and recieve one replay


sequence numbers in wireshark are given with
	relative numbers next to them + next expected ack number
	(relative - count from zero)
	
TCP connection seq and ack number progression

TCP Session
Connection Establishment
```
Client                              Server
  │                                   │
  │── SYN ──────────────────────────► │
  │   seq=100, ack=0                  │  Client picks random ISN=100
  │                                   │  SYN consumes 1 → next seq=101
  │                                   │
  │◄─ SYN-ACK ────────────────────── │
  │   seq=300, ack=101                │  Server picks random ISN=300
  │                                   │  ack=101 → "got 100, send 101 next"
  │                                   │  SYN consumes 1 → next seq=301
  │                                   │
  │── ACK ──────────────────────────► │
  │   seq=101, ack=301                │  Client acks server's SYN
  │                                   │  ack=301 → "got 300, send 301 next"
  │                                   │  pure ACK → seq stays 101
```

Data Transfer
```
Client                              Server
  │                                   │
  │── DATA (100 bytes) ─────────────► │
  │   seq=101, ack=301                │  sending bytes 101→200
  │                                   │
  │◄─ ACK ────────────────────────── │
  │   seq=301, ack=201                │  "got up to 200, send 201 next"
  │                                   │
  │◄─ DATA (50 bytes) ──────────────  │
  │   seq=301, ack=201                │  server sends bytes 301→350
  │                                   │
  │── ACK ──────────────────────────► │
  │   seq=201, ack=351                │  "got up to 350, send 351 next"
  │                                   │
  │── DATA (200 bytes) ─────────────► │
  │   seq=201, ack=351                │  sending bytes 201→400
  │                                   │
  │◄─ ACK ────────────────────────── │
  │   seq=351, ack=401                │  "got up to 400, send 401 next"
```


craft a packet manually with scapy
```
from scapy.all import *

packet = IP(dst="172.217.17.206", ttl=128, flags="DF") / TCP(
    sport=2020,
    dport=80,
    flags="S",
    seq=0,
    window=65535
)

send(packet)
```

flag = S on TCP layer means Synchronize, used to initiate a handshake
	synchronize sequence numbers
	only the first packet sent from each end should have this flag set
	
flag = DF on IP layer means do not fragment


What is the purpose of sending a packet with the RST flag after injecting a request?
	to stop the server from retransmitting data after the injected request.
	after injection the **real client is a problem** — it will receive confused responses and send its own RST, breaking everything. The attacker sends RST **preemptively** to silence the real client and cleanly take over the session


ACK flag has to be set to TRUE for successful packet injection in an already established TCP connection
